<nav class="navbar navbar-default navbar-wagon" role="navigation">
      <div class="row">
        <div class="margin-home">
          <div class="col-md-2" >
            <a class="navbar-brand" href="/">
              <%= "Home" %>
            </a>
          </div>
        </div>
        <div class="margin-embnw">
          <div class="col-md-7" >
            <h1 class="text-center margin-embnw">
              <i>Early Modern Beings and Writing</i>
            </h1>
          </div>
        </div>
        <div class="text-right">
        <div class="col-md-3" >
          <% if user_signed_in? %>
            <%= link_to "#" do %>
                <i class="fa fa-envelope-o"></i> <%= t(".messages", default: "Messages") %>
            <% end %>
            <%= link_to "#", {class: "dropdown-toggle", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
                <%= image_tag "http://placehold.it/30x30", class: "img-avatar" %>
                Profile <span class="caret"></span>
            <% end %>
            <ul class="dropdown-menu " role="menu">
              <li>
                <%= link_to "#" do %>
                  <i class="fa fa-user"></i> <%= t ".profile", default: "Profile" %>
                <% end %>
              </li>
              <li>
                <%= link_to "#" do %>
                  <i class="fa fa-home"></i>  <%= t ".profile", default: "Flat" %>
                <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t ".sign_out", default: "Sign out" %>
                <% end %>
              </li>
            </ul>
          <% else %>
            <%= link_to t(".sign_up", default: "Inscription"), new_user_registration_path %>
            <%= link_to t(".sign_in", default: "Connection"), new_user_session_path %>
          <% end %>
        </div>
      </div>
  </div>
</nav>
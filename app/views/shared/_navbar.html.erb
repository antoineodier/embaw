<nav class="navbar navbar-default navbar-wagon" role="navigation">
      <div class="row">
        <div class="margin-home">
          <div class="col-md-2" >
            <a class="navbar-brand" href="/">
              <%= "Home" %>
            </a>
          </div>
        </div>
        <div class="margin-embnw">
          <div class="col-md-7" >
            <h1 class="text-center">
              <i>Early Modern Beings and Writings</i>
              <h4 class="text-center">READING AND RESEARCHING EGODOCUMENTS
                <br>THROUGH A DIGITAL APPROACH</h4>
              <!-- margin-embnw -->
            </h1>
          </div>
        </div>
        <div class="text-right forme-texte">
        <div class="col-md-3" >
          <% if user_signed_in? %>
            <%= link_to "#", :class => "forme-texte" do %>
                <i class="fa fa-envelope-o forme-texte"></i> <%= t(".messages", default: "Envoyer un email") %>
            <% end %>
            <%= link_to "#", {class: "dropdown-toggle forme-texte", "data-toggle forme-texte" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
                <%= image_tag "divers/profile-pic.jpg", class: "img-avatar" %>
                Profil utilisateur <span class="caret"></span>
            <% end %>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to "#" do %>
                  <i class="fa fa-user"></i> <%= t ".profile", default: "Profil utilisateur" %>
                <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t ".sign_out", default: "Déconnection" %>
                <% end %>
              </li>
            </ul>
          <% else %>
            <%= link_to t(".sign_up", default: "Inscription"), new_user_registration_path %>
            <%= link_to t(".sign_in", default: "Connection"), new_user_session_path %>
          <% end %>
        </div>
      </div>
  </div>
</nav>